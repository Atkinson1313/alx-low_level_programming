#include <stdio.h>
/**
 * main - start point
 *
 * Description: use only putchar and nor more than twice
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int c;

	c = 0;
	while
		(c < 10)
		{
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
